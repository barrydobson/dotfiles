{{- if or (eq .chezmoi.os "linuxo") (eq .chezmoi.os "darwino") -}}
#! /usr/bin/env bash

set -ev pipefail

if [[ $(awk -F ":${HOME}:" 'NF>1 {print $2}' /etc/passwd) != $(which zsh) ]]; then
  sudo chsh -s $(which zsh) $(whoami)
fi

{{- end -}}